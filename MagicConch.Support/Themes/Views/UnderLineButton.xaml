<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cnvtrs="clr-namespace:MagicConch.Supoprt.Converters"
    xmlns:units="clr-namespace:MagicConch.Support.Themes.Units">
    
    <cnvtrs:NegativeValueConverter x:Key="NegativeValueConverter"/>
    
    <Style TargetType="units:UnderLineButton">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="BorderBrush" Value="Black" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="units:UnderLineButton">
                    <StackPanel Orientation="Vertical">
                        <ContentPresenter />
                        <Border
                            x:Name="underline"
                            Width="{Binding RelativeSource={RelativeSource AncestorType=units:UnderLineButton}, Path=ActualWidth}"
                            Height="1"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <Border.RenderTransform>
                                <TranslateTransform x:Name="underlineTransform" />
                            </Border.RenderTransform>
                        </Border>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="underlineTransform"
                                            Storyboard.TargetProperty="X"
                                            To="{Binding RelativeSource={RelativeSource AncestorType=units:UnderLineButton}, Path=ActualWidth}"
                                            Duration="0:0:0.5">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="underlineTransform"
                                            Storyboard.TargetProperty="X"
                                            From="{Binding RelativeSource={RelativeSource AncestorType=units:UnderLineButton}, Path=ActualWidth, Converter={StaticResource NegativeValueConverter}}"
                                            To="0"
                                            Duration="0:0:0.5">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </StackPanel>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>